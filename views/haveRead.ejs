<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>haveRead list</title>

</head>
<body>
  <div id="todo-table">
    <form>
      <input type="text" name='item' placeholder="Add new item..." required />
      <button type="submit">Add Item</button>
    </form>
    <ul>
      <% haveRead.forEach(function(haveRead) { %>
        <li><%= haveRead.item %></li>
      <% }) %>
    </ul>
  </div>
</body>
</html>